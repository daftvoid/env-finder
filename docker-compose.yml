services:
    scraper:
        build:
            dockerfile: Dockerfile
            context: .
        image: env-finder:1.0
        ports:
            - "6767:6767"
        volumes:
            - ./data:/data
        env_file:
            - .env